This repo contains an implementation of a decentralized ad hoc communication system based on a LoRa mesh network that is augmented with bluetooth low energy based smartphone application that provides a UI. The system supports short messages communication  between users connected to  nodes that are either are reachable via a one hop direct link or are reachable via other intermediary links which route the traffic to the intended destination. 
